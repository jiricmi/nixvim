{
  plugins = {
    mini = {
      enable = true;

      modules = {
        surround = { };
      };
    };
    treesj = {
      enable = true;

    };
    colorizer = {
      enable = true;
    };
  };
}
